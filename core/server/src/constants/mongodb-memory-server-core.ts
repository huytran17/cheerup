const MongoMemoryServerCoreState = {
  NEW: "new",
  STOPPED: "stopped",
  RUNNING: "running",
};

export default Object.freeze({
  MongoMemoryServerCoreState,
});

export { MongoMemoryServerCoreState };
