import signInRules from "./sign-in";
import signUpRules from "./sign-up";
import signOutRules from "./sign-out";
import verifyAccessRules from "./verify-access";

export default Object.freeze({
  signInRules,
  signUpRules,
  signOutRules,
  verifyAccessRules,
});

export { signInRules, signUpRules, signOutRules, verifyAccessRules };
