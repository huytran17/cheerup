import signInRules from "./sign-in";
import signUpRules from "./sign-up";
import verifyAccessRules from "./verify-access";

export default Object.freeze({
  signInRules,
  signUpRules,
  verifyAccessRules,
});

export { signInRules, signUpRules, verifyAccessRules };
