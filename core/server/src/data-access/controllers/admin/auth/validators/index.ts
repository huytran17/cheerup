import signInRules from "./sign-in";
import signUpRules from "./sign-up";
import signOutRules from "./sign-out";

export default Object.freeze({
  signInRules,
  signUpRules,
  signOutRules,
});

export { signInRules, signUpRules, signOutRules };
