import signInRules from "./sign-in";
import signOutRules from "./sign-out";
import verifyAccessRules from "./verify-access";

export default Object.freeze({
  signInRules,
  signOutRules,
  verifyAccessRules,
});

export { signInRules, signOutRules, verifyAccessRules };
